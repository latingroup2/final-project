<!-- Validation failure messages -->
<% if @colegio.errors.any? %>
  <% @colegio.errors.full_messages.each do |message| %>
    <div class="alert">
      <%= message %>
    </div>
  <% end %>
<% end %>

<h1>
  Edit colegio #<%= @colegio.id %>
</h1>

<hr>

<form action="/update_colegio/<%= @colegio.id %>" method="post">
  <!-- Label and input for nombre -->
  <div class="form-group">
    <label for="nombre">
      Nombre
    </label>

    <textarea id="nombre" name="nombre" class="form-control" rows="3"><%= @colegio.nombre %></textarea>
  </div>

  <button class="btn btn-block btn-outline-secondary">
    Update colegio
  </button>
</form>

<hr>

<a href="/colegios">
  Go back
</a>
